README
A YARN cluster can run many applications.
An application uses one container as its applicaiton master.
An application master can try a number of application attempts as configured in 
yarn.resourcemanager.am.max-attempts (default 2).
