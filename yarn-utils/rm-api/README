README
A YARN cluster can run many applications.
An application uses one container as its applicaiton master.
An application has a list of application attempts.
TBD: Each application attempt use a container under a node manager. 
